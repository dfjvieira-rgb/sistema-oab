<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>ARYANNA 2026 | MENTORIA</title>
    <link href="https://fonts.googleapis.com" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com">
    <style>
        :root { --bg: #f1f5f9; --text: #000; --primary: #1d4ed8; --gold: #fbbf24; --border: #94a3b8; --purple: #5b21b6; --success: #065f46; --danger: #991b1b; }
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: 'Inter', sans-serif; background: var(--bg); color: var(--text); padding-bottom: 50px; }
        header { background: #000; border-bottom: 3px solid var(--gold); padding: 15px 20px; display: flex; justify-content: space-between; align-items: center; position: sticky; top: 0; z-index: 1000; }
        .btn-header { text-decoration: none; color: var(--gold); font-weight: 800; border: 2px solid var(--gold); padding: 8px 15px; border-radius: 8px; display: flex; align-items: center; gap: 8px; font-size: 0.8rem; cursor: pointer; background: transparent; }
        .search-area { padding: 15px; background: #fff; border-bottom: 2px solid var(--border); }
        #searchBar { width: 100%; padding: 12px; border: 2px solid var(--border); border-radius: 8px; font-weight: 700; }
        .snap-section { padding: 15px; background: rgba(0,0,0,0.05); }
        .snap-carousel { display: flex; gap: 15px; overflow-x: auto; padding-bottom: 15px; scroll-snap-type: x mandatory; }
        .snap-card { min-width: 320px; background: #fff; border: 2px solid var(--gold); border-radius: 12px; padding: 15px; scroll-snap-align: start; position: relative; }
        .dashboard { display: grid; grid-template-columns: repeat(2, 1fr); gap: 15px; padding: 15px; }
        .panel { background: #fff; border-radius: 12px; border: 2px solid var(--border); display: flex; flex-direction: column; margin-bottom: 15px; height: fit-content; }
        .panel-header { padding: 12px; background: #f8fafc; border-bottom: 2px solid var(--border); font-size: 0.8rem; font-weight: 800; text-transform: uppercase; text-align: center; }
        .panel-body { padding: 15px; }
        label { font-size: 0.7rem; font-weight: 800; text-transform: uppercase; display: block; margin-bottom: 4px; color: #475569; }
        input, textarea { width: 100%; border: 2px solid var(--border); border-radius: 8px; padding: 12px; font-size: 1rem; margin-bottom: 10px; font-family: inherit; }
        .btn-save { width: 100%; padding: 15px; border-radius: 8px; border: none; font-weight: 800; font-size: 0.9rem; cursor: pointer; color: #fff; text-transform: uppercase; }
        .feed { margin-top: 15px; max-height: 550px; overflow-y: auto; border-top: 1px solid var(--border); padding-top: 10px; }
        .card { background: #fff; padding: 15px; border-radius: 8px; border: 2px solid var(--border); margin-bottom: 10px; position: relative; }
        .card-pre { border-left: 8px solid var(--success); }
        .btn-edit-tool { position: absolute; top: 10px; right: 10px; display: flex; gap: 12px; }
        .btn-edit-tool i { cursor: pointer; opacity: 0.4; font-size: 1rem; }
        .btn-reveal { background: #000; color: #fff; border: none; padding: 12px; border-radius: 6px; font-weight: 800; width: 100%; cursor: pointer; margin-top: 5px; }
        .hidden-content { display: none; margin-top: 10px; padding: 12px; background: #dcfce7; border: 1px solid var(--success); border-radius: 6px; font-weight: 700; }
        @media (max-width: 900px) { .dashboard { grid-template-columns: 1fr; } }
    </style>
</head>
<body>
<header>
    <div class="btn-header" onclick="window.history.back()"><i class="fas fa-arrow-left"></i> VOLTAR</div>
    <div style="color:white; font-weight: 800; font-size: 0.9rem;">ARYANNA 2026 | MENTORIA</div>
    <button class="btn-header" style="background: var(--success); color: white; border: none;" onclick="baixarBackup()"><i class="fas fa-download"></i> BACKUP</button>
</header>
<div class="search-area"><input type="text" id="searchBar" placeholder="🔍 CONSULTAR FIREBASE..." onkeyup="filtrar()"></div>
<section class="snap-section"><label><i class="fas fa-rocket"></i> Histórico de Envios (Snapshots)</label><div class="snap-carousel" id="feed-snaps"></div></section>
<main class="dashboard">
    <section class="panel" style="border-top: 6px solid var(--success);"><div class="panel-header">Preliminares</div><div class="panel-body"><input type="hidden" id="id-pre"><label>Título</label><input type="text" id="pre-t"><label>Fundamentação</label><textarea id="pre-c"></textarea><button class="btn-save" style="background: var(--success);" onclick="salvar('pre')">Salvar/Atualizar</button><div class="feed" id="feed-pre"></div></div></section>
    <section class="panel" style="border-top: 6px solid var(--purple);"><div class="panel-header">Qual é a Peça?</div><div class="panel-body"><input type="hidden" id="id-peca"><label>Enunciado</label><textarea id="peca-t"></textarea><label>Gabarito</label><input type="text" id="peca-c"><button class="btn-save" style="background: var(--purple);" onclick="salvar('peca')">Salvar Desafio</button><div class="feed" id="feed-peca"></div></div></section>
    <section class="panel" style="border-top: 6px solid var(--danger);"><div class="panel-header">Radar</div><div class="panel-body"><input type="hidden" id="id-nc"><label>Conceito</label><input type="text" id="nc-t"><label>Diferença</label><textarea id="nc-c"></textarea><button class="btn-save" style="background: var(--danger);" onclick="salvar('nc')">Salvar Radar</button><div class="feed" id="feed-nc"></div></div></section>
    <section class="panel" style="border-top: 6px solid var(--primary);"><div class="panel-header">Arquivo de Teses</div><div class="panel-body"><input type="hidden" id="id-tese"><label>Título</label><input type="text" id="tese-t"><label>Conteúdo</label><textarea id="tese-c"></textarea><button class="btn-save" style="background: var(--primary);" onclick="salvar('tese')">Arquivar Tese</button><div class="feed" id="feed-tese"></div></div></section>
</main>
<script type="module">
    import { initializeApp } from "https://www.gstatic.com";
    import { getDatabase, ref, push, set, onValue, remove, serverTimestamp } from "https://www.gstatic.com";
    const config = { apiKey: "AIzaSyAmigODFK8R9c0-fWtagdxLWu9xkODfKYQ", authDomain: "masteroab-db5e1.firebaseapp.com", projectId: "masteroab-db5e1", databaseURL: "https://masteroab-db5e1-default-rtdb.firebaseio.com" };
    const app = initializeApp(config); const db = getDatabase(app);
    const paths = { pre: 'v13_pre', peca: 'v13_peca', nc: 'v13_nc', tese: 'v13_tese', snaps: 'historico_envios' };
    window.toggleR = (btn) => { const div = btn.nextElementSibling; div.style.display = (div.style.display === "block" ? "none" : "block"); };
    window.salvar = (type) => { const id = document.getElementById(`id-${type}`).value; const t = document.getElementById(`${type}-t`).value; const c = document.getElementById(`${type}-c`).value; if(!t || !c) return; const data = { t, c, ts: serverTimestamp() }; if(id) set(ref(db, `${paths[type]}/${id}`), data); else push(ref(db, paths[type]), data); document.getElementById(`id-${type}`).value = ''; document.getElementById(`${type}-t`).value = ''; document.getElementById(`${type}-c`).value = ''; };
    window.editar = (type, id, t, c) => { document.getElementById(`id-${type}`).value = id; document.getElementById(`${type}-t`).value = t; document.getElementById(`${type}-c`).value = c; window.scrollTo(0,0); };
    window.excluir = (p, id) => { if(confirm("Apagar?")) remove(ref(db, `${paths[p]}/${id}`)); };
    window.excluirSnap = (ex, id) => { if(confirm("Apagar?")) remove(ref(db, `historico_envios/${ex}/${id}`)); };
    window.filtrar = () => { const q = document.getElementById('searchBar').value.toLowerCase(); document.querySelectorAll('.card, .snap-card').forEach(c => { c.style.display = c.innerText.toLowerCase().includes(q) ? 'block' : 'none'; }); };
    window.baixarBackup = () => { onValue(ref(db), (snap) => { const b = new Blob([JSON.stringify(snap.val(), null, 2)], {type: 'application/json'}); const a = document.createElement('a'); a.href = URL.createObjectURL(b); a.download = `BACKUP_ELITE_${new Date().toISOString().slice(0,10)}.json`; a.click(); }, { onlyOnce: true }); };
    onValue(ref(db), (snap) => {
        const v = snap.val() || {}; const fS = document.getElementById('feed-snaps'); fS.innerHTML = "";
        const h = v[paths.snaps] || {};
        Object.keys(h).forEach(ex => {
            Object.entries(h[ex]).reverse().forEach(([id, info]) => {
                fS.innerHTML += `<div class="snap-card"><div class="btn-edit-tool"><i class="fas fa-trash" onclick="window.excluirSnap('${ex}','${id}')"></i></div><span style="font-size:0.6rem; font-weight:800; background:#000; color:#fbbf24; padding:2px 5px; border-radius:4px;">EXAME ${ex}</span><small style="display:block; font-size:0.6rem; margin-top:5px; opacity:0.6">${info.data || ''}</small><strong style="display:block; margin:5px 0;">${info.peca_nome || 'PEÇA'}</strong><div style="font-size:0.75rem; color:#475569; height:80px; overflow-y:auto; white-space: pre-wrap; background:#f8fafc; padding:5px; border-radius:4px;">${info.conteudo || ''}</div></div>`;
            });
        });
        const render = (type, target) => { const f = document.getElementById(target); f.innerHTML = ""; const obj = v[paths[type]] || {}; Object.keys(obj).map(id => ({ id, ...obj[id] })).sort((a, b) => a.t.localeCompare(b.t)).forEach(item => { f.innerHTML += `<div class="card ${type==='pre'?'card-pre':''}"> <div class="btn-edit-tool"> <i class="fas fa-pen" onclick="editar('${type}','${item.id}','${item.t.replace(/'/g, "\\'")}','${item.c.replace(/'/g, "\\'")}')"></i> <i class="fas fa-trash" onclick="excluir('${type}','${item.id}')"></i> </div> ${type==='peca' ? `<p style="font-weight:700;">${item.t}</p><button class="btn-reveal" onclick="toggleR(this)">VER GABARITO</button><div class="hidden-content">${item.c}</div>` : `<strong>${item.t}</strong><p>${item.c}</p>`} </div>`; }); };
        render('pre', 'feed-pre'); render('peca', 'feed-peca'); render('nc', 'feed-nc'); render('tese', 'feed-tese');
    });
</script>
</body>
</html>
